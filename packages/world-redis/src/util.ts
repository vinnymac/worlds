import { createDebugLogger } from '@fantasticfour/utils';

export { compact, Mutex, Rc } from '@fantasticfour/utils';
export { parse, stringify } from '@fantasticfour/utils';
export const debug = createDebugLogger('redis-world');
