---
'@fantasticfour/world-firestore-tasks': patch
---

Bump `@google-cloud/firestore` to ^9.0.1 (from ^7.10.0) and `@google-cloud/tasks` to ^7.0.0 (from ^6.2.1). Both majors raise the minimum Node.js version to 22, which this repo already meets. The API surface we use didn't change, so no source changes were needed.
